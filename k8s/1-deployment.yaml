apiVersion: apps/v1
kind: Deployment
metadata:
  name: ferrea-libraries
  namespace: ferrea-libraries
  labels:
    app: ferrea-libraries
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ferrea-libraries
  template:
    metadata:
      labels:
        app: ferrea-libraries
    spec:
      containers:
        - name: ferrea-libraries
          image: grimoldi/ferrea-libraries:1.0
          ports:
            - containerPort: 80
